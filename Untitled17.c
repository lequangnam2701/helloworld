#include <stdio.h>

int main() {
	if(sum > = 40)
	scanf("%f%f%f",&t,&v,&a);
	if (0 <= t,v,a <= 10) {
		float SUM = t*2 + v*2 + a;
	if (SUM >= 40) {
		printf("PASS");
	} else {
	  	printf("FAIL");
		
	}
    return 0;	
	


